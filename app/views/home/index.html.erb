
            <div class="hero-body">
                <div class="container has-text-centered">
                    <h1 class="title">
                    Shorten that long URL into a tiny Url </h1>
                    <h2 class="subtitle">
                    Kapyenga helps turn long urls into short memorable Urls.
                    </h2>
                      <% unless user_signed_in? %>
                     <%= link_to 'Sign Up', new_user_registration_path, class: "button is-success is-rounded"  %>
                      &nbsp &nbsp or &nbsp &nbsp
                     <%= link_to 'Login', new_user_session_path, class: "button is-success is-rounded"  %>
                      <% end %>
                      <% if user_signed_in? %>
                     <%= link_to 'Create Link', new_link_path, class: "button is-success is-rounded"  %>
                     <% end %>
                </div>
            </div>
